import '../App.css';

export default function StockDisplay(props){
    return(
        <div>
            {/* {props.display !== null && props.data.length !== 0
            ? <div>
                <h2> Top WSB comments for {props.data[props.data.length-1].Name} </h2>
                <p> Avg. Daily: {props.display.mentions} comments </p>
                <p> Top comments: {props.display.comments} </p>
              </div>
            : ""}  */}
        </div>
    )
}